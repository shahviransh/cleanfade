{
  "name": "cleanfade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "build:sidecar": "powershell -ExecutionPolicy Bypass -File ./scripts/build_sidecar.ps1",
    "tauri:dev": "vite && npm run build:sidecar && tauri dev",
    "tauri:build": "vite build && npm run build:sidecar && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-react": "^4.5.2",
    "vite": "^5.4.20"
  }
}
